This exerice will allow me to show my skills:
- Using OpenSSL to connect to TLS-protected services and inspecting the handshake
- Read and interpret certificates
- Identify weak TLS configuration using command-line tools
- Set up a local TLS server and client using OpenSSL

Task 1: Connecting to a TLS Server with s_client
Using s_client -connect and -brief in the comman line allows for the ovbervation of the handshake, which shows the ceritifcate chain the server certificate and handshake parameters. By puting the certificate contentance in a file and them making readable by using the command line openssl x509 -in file.pem -text -noout , the certificate is human readable. In order to have a look at a real certificate, I used google.com's certificate. All below questions will be answered based on that certificate.

Questions:
1. What TLS version was negotiated? What cipher was used?
The negotiated version referes to the highest TLS version supported by both the server and the client. In this case this is case it is TLSv1.3 as shown in the line Protocol version: TLSv1.3. Cipher used can be seen in the output ciphersuite: TLS_AES_256_GCM_SHA384

2. Who is the Certificate Authority (CA) that issued the certifictae? Is it a root CA or an intermediate CA? 
Under CA Issuer in the .pem file, the certificate that is downloaded shows it is issues by GTS Root R1. Tipically an intermediate CA is used to communicate between server certificate and root CA.

3. What is the public key algorithm and key size? Is this considered secure by current standards?
Public key size: 256 bit and key algorithm is ECDSA wih P-256. This is considered highly secure.

4. What are the Subjects Alternative Names (SANs)? Why are SANs important rather than replying solely on the Common Name (CN)?
SAN alows for multiple domain names to be secure through a single certificate. In this case under SAN there is only www.google.com. SANs are better it allows for multiple names whist CN only allows for one.

5. What is the certificate's validity period? What would happen if you visited the site after the expiry date?
This certificates valididate date is until 20/02/2029. With an expired certificate, the site will show a not secure warning and automated tools such as APIs will not work, even if you are able to acess the website.

Task 2: Certificate chain verification

Questions:
1. How many certificates are in the chain for the site you tested?
1st certificate: server, www.google.com 
2nd certificate: Inntermeditate, GTS Root R1
3rd certificate: Root, GTS Root R1

2. Why does the server send intermediate certificates but not the root? What would 
happen if the intermediate were missing?
The root certificate should already be in the cient's trust, so there it no need for the root to be sent. If the intermediate certificate is not sent, the certificate chain breaks and verification fails

3. How many root CAs does your system trust? What are the security implications of 
this number being large?
My system trusts 140 CAs. The security issues come from the fct that these CAs can give certificates to any domain, and if one of these CAs are compormised,they can allow fradulent certificates to be allowed

4. What is the difference between a root CA and an intermediate CA? Why do CAs use 
intermediates rather than signing directly with the root?
Intermidiate CA act as a middle man to help with security. The security of the root CA is very important and if the imtermidiate CA is compromised, it can be removed without having to also replace the root.

Task 3: Creating a Self-Signed Certificate and Private CA

Questions:
1. What is the difference between a self-signed certificate and one signed by a CA? In 
what scenarios is each appropriate?
There is no 3rd party verifying the certificate and should also only be used for testing and development. A certificate signed by a CA means a third party signature on a certificate, which means is more secure for public facing services.

2. Why should the CA’s private key be larger (4096-bit) than the server’s key (2048-
bit)?
Bigger keys allows for a longer secure lifetime, as CAs are not changed as often as server certificates are.


3. What is a CSR, and what information does it contain? Why doesn’t the CSR contain 
the private key?
CSR: certificate signing request. 

4.  If you distributed your ca.pem to colleagues and they added it to their trust stores, 
what would be the effect? What risks would this introduce?
All certificates that aere signed with my CA would be trusted. However,if this is compormised, attackers could give a certificate for any malious domain.

Task 4: Running a Local TLS Server and Client

Question:
1. What was the verification result with and without the -CAfile flag? What does this tell 
you about how trust works in TLS?

2. Why does curl reject the connection by default? What does the -k flag do, and why is 
it dangerous in production?

3. What differences did you observe between TLS 1.3 and TLS 1.2 in the handshake 
output?

Task 5: Auditing TLS Configurations

Questions:
1. Does the server support TLS 1.1 or TLS 1.0? If so, what attacks does this enable?


2. What is the weakest cipher suite offered? Is it acceptable?


3. Is OCSP stapling enabled? Why is OCSP stapling preferable to clients contacting the 
OCSP responder directly?


4. Based on your audit, what grade (A–F) would you give the site’s TLS configuration, 
and why?

